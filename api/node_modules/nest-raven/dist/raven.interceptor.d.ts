import { ExecutionContext, NestInterceptor, CallHandler } from '@nestjs/common';
import { IRavenInterceptorOptions } from './raven.interfaces';
import { Observable } from 'rxjs';
import { Reflector } from '@nestjs/core';
export declare class RavenInterceptor implements NestInterceptor {
    private readonly options;
    private readonly reflector;
    constructor(options?: IRavenInterceptorOptions, reflector?: Reflector);
    intercept(context: ExecutionContext, next: CallHandler): Observable<any>;
    private addGraphQLExceptionMetadatas;
    private addHttpExceptionMetadatas;
    private addRpcExceptionMetadatas;
    private addWsExceptionMetadatas;
    private captureException;
    private shouldReport;
}
